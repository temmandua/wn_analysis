03.11.25
						2 MAIN TASK CLUSTERS:

1. OPUS corpus data acquisition
	a. get familiar with OPUS
	b. write code to obtain 50m random sentences per language
		prof recommended unix tools for random (something with sort(), ...)
	c. get familiar with spaCy / NLTK and write code for lemmatization

2. WordNet organization
	a. figure out how to read in other WordNets:
		Option 1: write code to convert to WN-LMF 1.3 format
		...other options?
	b. figure out if there is mapping to English, or a way to do so
		- how did the creators of the WordNets do this?
		- if not... what to do then?

########################################################################
7.11.25

protocol:

tried doing it with some pure python code (random.sample()), was too time- and processor-consuming
sort -R with git bash for Windows worked faster and easier... but need to check for identical sentences

	sort -R /pathTo/originalSentenceFile.txt | head -n 50000000 > 	/pathTo/sampleFile.txt

sort <file> < uniq --count showed lots of rows with different languages in them ... can we deal with that somehow?

	sort -R /pathTo/originalSentenceFile.txt | head -n 50000000 > 	/mnt/c/pathTo/sampleFile.txt

if we don't clean it up, like removing different language etc, we should account for it in some way, like at last mention



what would be the most efficient way to store the words in?

- dictionary? we could actually search via the key 'lemma' and 'POS' and maybe 'meaning' to make sure it fits


########################################################################
8.11.25

protocol:

...for now let's focus on the WordNets, since this will probably depend on them...


########################################################################

QUESTIONS:

1 do we need to check for the language beforehand? The frequency searching will probably be enough since it will not show up in the specific WordNets anyway
